/* Placeholder agent workspace. The AI queue director is a future capability;
   this is the destination the radial menu's Agent node will eventually open. */
export function AgentQueueDirectorWorkspace() {
    return (
        <div className="sap-ctl__workspace-empty">
            <p className="sap-ctl__workspace-lead">AI queue director coming soon</p>
            <p className="sap-ctl__workspace-sub">
                Let an agent curate and reorder what plays next.
            </p>
        </div>
    )
}

export default AgentQueueDirectorWorkspace
